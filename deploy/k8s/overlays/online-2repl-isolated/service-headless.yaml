apiVersion: v1
kind: Service
metadata:
  name: yikv-server-hl
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: yikv-server
  ports:
    - name: rpc
      port: 9000
      targetPort: rpc
      protocol: TCP
